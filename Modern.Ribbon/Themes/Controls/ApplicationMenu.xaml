<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Modern="clr-namespace:Modern"
                    xmlns:ModernHelpers="clr-namespace:Modern.Helpers"
                    xmlns:converters="clr-namespace:Modern.Converters"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:styleSelectors="clr-namespace:Modern.StyleSelectors"
                    mc:Ignorable="d">
    <ControlTemplate x:Key="Modern.Ribbon.Templates.ApplicationMenuButton"
                     TargetType="{x:Type Modern:ApplicationMenu}">
        <Grid Margin="0">
            <ContentControl x:Name="buttonContent"
                            Width="{Binding ActualWidth, ElementName=PART_ButtonBorder}"
                            Height="{Binding ActualHeight, ElementName=PART_ButtonBorder}"
                            Margin="0"
                            Focusable="False">
                <Border x:Name="PART_ButtonBorder"
                        MinWidth="60"
                        Background="{TemplateBinding Background}">
                    <ContentControl x:Name="contentPresenter"
                                    Margin="10 0 10 0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Content="{TemplateBinding Header}"
                                    Focusable="False" />
                </Border>
            </ContentControl>
            <Popup x:Name="PART_Popup"
                   Grid.ColumnSpan="2"
                   MaxHeight="{TemplateBinding MaxDropDownHeight}"
                   Margin="0 0 -4 0"
                   VerticalAlignment="Top"
                   AllowsTransparency="True"
                   CustomPopupPlacementCallback="{x:Static ModernHelpers:PopupHelper.SimplePlacementCallback}"
                   Focusable="False"
                   IsOpen="{TemplateBinding IsDropDownOpen}"
                   Placement="Custom"
                   PlacementTarget="{Binding ElementName=buttonContent}"
                   VerticalOffset="{Binding ElementName=buttonContent, Path=ActualHeight}">
                <Grid Name="DropDown"
                      Margin="0"
                      VerticalAlignment="Top"
                      SnapsToDevicePixels="True">
                    <Border x:Name="PART_DropDownBorder"
                            MinWidth="{TemplateBinding ActualWidth}"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Top"
                            Background="{DynamicResource Modern.Ribbon.Brushes.DropDown.Background}"
                            BorderBrush="{DynamicResource Modern.Ribbon.Brushes.DropDown.Border}"
                            BorderThickness="1"
                            SnapsToDevicePixels="True">
                        <Grid HorizontalAlignment="Stretch"
                              VerticalAlignment="Top">
                            <Grid HorizontalAlignment="Stretch"
                                  VerticalAlignment="Top">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto"
                                                   MinHeight="17" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"
                                                      MinWidth="100" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <ScrollViewer Style="{DynamicResource Modern.Ribbon.Styles.MenuScrollViewer}"
                                              Grid.Row="0"
                                              Grid.Column="0"
                                              IsTabStop="False"
                                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                                    <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Cycle"
                                                Orientation="Vertical"
                                                ScrollViewer.VerticalScrollBarVisibility="Auto" />
                                </ScrollViewer>

                                <Border Grid.Row="0"
                                        Grid.Column="1"
                                        Width="{TemplateBinding RightPaneWidth}"
                                        BorderBrush="{DynamicResource Modern.Ribbon.Brushes.Separator.Border}"
                                        BorderThickness="1 0 0 0">
                                    <ScrollViewer x:Name="PART_ScrollViewer"
                                                  Style="{DynamicResource Modern.Ribbon.Styles.MenuScrollViewer}"
                                                  Margin="1"
                                                  IsTabStop="False"
                                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                                  SnapsToDevicePixels="True">
                                        <ContentPresenter x:Name="PART_RightContentPresenter"
                                                          ContentSource="RightPaneContent" />
                                    </ScrollViewer>
                                </Border>
                                <Border Grid.Row="1"
                                        Grid.ColumnSpan="2"
                                        Margin="0"
                                        Background="{DynamicResource Modern.Ribbon.Brushes.Ribbon.Background}"
                                        BorderBrush="{DynamicResource Modern.Ribbon.Brushes.Separator.Border}"
                                        BorderThickness="0 1 0 0">
                                    <ContentPresenter ContentSource="FooterPaneContent" />
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>
            </Popup>
        </Grid>

        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="PART_ButtonBorder" Property="Opacity" Value="0.5" />
                <Setter TargetName="contentPresenter" Property="Effect">
                    <Setter.Value>
                        <Modern:GrayscaleEffect />
                    </Setter.Value>
                </Setter>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="Modern.Ribbon.Styles.ApplicationMenu"
           TargetType="{x:Type Modern:ApplicationMenu}">
        <Setter Property="Background" Value="{DynamicResource Modern.Ribbon.Brushes.AccentBase}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource Modern.Ribbon.Styles.FocusVisual}" />
        <Setter Property="Foreground" Value="{DynamicResource Modern.Ribbon.Brushes.IdealForeground}" />
        <Setter Property="Header" Value="{converters:ObjectToImageConverter {DynamicResource Modern.Ribbon.Images.ApplicationMenu}, '40,16', {Binding RelativeSource={RelativeSource Self}}}" />
        <Setter Property="ItemContainerStyleSelector" Value="{x:Static styleSelectors:ApplicationMenuItemContainerStyleSelector.Instance}" />
        <Setter Property="KeyboardNavigation.TabIndex" Value="1" />
        <Setter Property="Template" Value="{DynamicResource Modern.Ribbon.Templates.ApplicationMenuButton}" />
    </Style>
</ResourceDictionary>