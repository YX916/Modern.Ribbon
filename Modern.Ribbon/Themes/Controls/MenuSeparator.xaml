<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Modern="clr-namespace:Modern"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    mc:Ignorable="d">
    <Style x:Key="Modern.Ribbon.Styles.MenuSeparator"
           TargetType="{x:Type Separator}">
        <Setter Property="Template" Value="{DynamicResource Modern.Ribbon.Templates.MenuSeparator}" />
    </Style>

    <ControlTemplate x:Key="Modern.Ribbon.Templates.MenuSeparator"
                     TargetType="{x:Type Separator}">
        <Grid Height="Auto">
            <Path Height="1"
                  Margin="32 1 3 2"
                  VerticalAlignment="Center"
                  Data="M0,0L1,0"
                  Fill="{x:Null}"
                  Stretch="Fill"
                  Stroke="{DynamicResource Modern.Ribbon.Brushes.Separator.Border}"
                  StrokeDashArray="2 2" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="Modern.Ribbon.Styles.MenuGroupSeparator"
           TargetType="{x:Type Modern:GroupSeparatorMenuItem}">
        <Setter Property="Template" Value="{DynamicResource Modern.Ribbon.Templates.MenuGroupSeparator}" />
    </Style>

    <ControlTemplate x:Key="Modern.Ribbon.Templates.MenuGroupSeparator"
                     TargetType="{x:Type Modern:GroupSeparatorMenuItem}">
        <Grid Height="Auto"
              Background="{DynamicResource Modern.Ribbon.Brushes.GroupSeparator.Background}">
            <TextBlock Margin="7 0"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center"
                       FontWeight="Bold"
                       Text="{Binding Header, RelativeSource={RelativeSource TemplatedParent}}"
                       TextWrapping="Wrap" />
        </Grid>
    </ControlTemplate>
</ResourceDictionary>