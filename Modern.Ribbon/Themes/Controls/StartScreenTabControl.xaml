<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Modern="clr-namespace:Modern"
                    xmlns:converters="clr-namespace:Modern.Converters">
    <ControlTemplate x:Key="Modern.Ribbon.Templates.StartScreenTabControl"
                     TargetType="{x:Type Modern:StartScreenTabControl}">
        <ControlTemplate.Resources>
            <ResourceDictionary>
                <Style x:Key="{x:Type Modern:Button}"
                       TargetType="{x:Type Modern:Button}"
                       BasedOn="{StaticResource Modern.Ribbon.Styles.Backstage.Button}" />
                <Style x:Key="{x:Type Modern:DropDownButton}"
                       TargetType="{x:Type Modern:DropDownButton}"
                       BasedOn="{StaticResource Modern.Ribbon.Styles.Backstage.DropDownButton}" />
                <Style x:Key="{x:Type Modern:ComboBox}"
                       TargetType="{x:Type Modern:ComboBox}"
                       BasedOn="{StaticResource Modern.Ribbon.Styles.Backstage.ComboBox}" />
            </ResourceDictionary>
        </ControlTemplate.Resources>
        <Grid x:Name="grid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"
                                  MinWidth="{TemplateBinding ItemsPanelMinWidth}" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid x:Name="PART_LeftContentGrid"
                  Grid.Column="0"
                  Margin="{TemplateBinding LeftContentMargin}"
                  VerticalAlignment="Stretch"
                  Background="{TemplateBinding ItemsPanelBackground}">
                <ContentPresenter Margin="15 25"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  Content="{TemplateBinding LeftContent}" />
            </Grid>

            <Grid x:Name="PART_SelectedContentGrid"
                  Grid.Column="1"
                  Background="{TemplateBinding Background}">
                <ContentPresenter x:Name="PART_SelectedContentHost"
                                  Margin="33 16"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  Content="{TemplateBinding RightContent}" />
            </Grid>
        </Grid>
    </ControlTemplate>

    <Style x:Key="Modern.Ribbon.Styles.StartScreenTabControl"
           TargetType="{x:Type Modern:StartScreenTabControl}"
           BasedOn="{StaticResource Modern.Ribbon.Styles.BackstageTabControl}">
        <Setter Property="Template" Value="{DynamicResource Modern.Ribbon.Templates.StartScreenTabControl}" />
    </Style>
</ResourceDictionary>