<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ControlTemplate x:Key="Modern.Ribbon.Templates.FocusVisual.Empty"
                     TargetType="{x:Type Control}">
        <Rectangle Stroke="Transparent">
            <Rectangle.Fill>
                <SolidColorBrush />
            </Rectangle.Fill>
        </Rectangle>
    </ControlTemplate>

    <Style x:Key="Modern.Ribbon.Styles.FocusVisual.Rectangle"
           TargetType="{x:Type Rectangle}">
        <Setter Property="Margin" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Stroke" Value="{DynamicResource Modern.Ribbon.Brushes.Black}" />
        <Setter Property="StrokeThickness" Value="1" />
    </Style>

    <ControlTemplate x:Key="Modern.Ribbon.Templates.FocusVisual"
                     TargetType="{x:Type Control}">
        <Rectangle Style="{DynamicResource Modern.Ribbon.Styles.FocusVisual.Rectangle}" />
    </ControlTemplate>

    <Style x:Key="Modern.Ribbon.Styles.FocusVisual.Empty"
           TargetType="{x:Type Control}">
        <Setter Property="Template" Value="{DynamicResource Modern.Ribbon.Templates.FocusVisual.Empty}" />
    </Style>

    <Style x:Key="Modern.Ribbon.Styles.FocusVisual"
           TargetType="{x:Type Control}">
        <Setter Property="Template" Value="{DynamicResource Modern.Ribbon.Templates.FocusVisual}" />
    </Style>
</ResourceDictionary>